BUILD IT RIGID FIRST BEFORE MAKING DYNAMIC WITH AI AND ALLOWING TYPING AND STUFF

* Currency should default to USD

* Filter user groups based on (fitness_goals, health_conditions, allergies, preferred_cuisine - only pick one or non at a time so the combination doesn't get so many) and recommend 4 meals to them but make sure that the two being picked was not recommended to them the previous day or they have marked it as "not liked" or the price is way above their average in a case where they is less than 2 options left or they have not being filtered into a group then manually generate recommendations for them. So I will check the recommendation model and see people who haven't been offered a recommendation then recommend to them. (auto using CRON JOB)

* Only recommend for people who are active (Meaning they have replied our message in the last 24 hours)

* When it's been 22 hours and almost 24 hours since they replied our message last send then send a message telling them that if they don't confirm we will stop sending them recommend till they message or reply us. (auto using CRON JOB)
  -- Hi it's important you respond so we know you still want recommendation if you don't we might stop till you message us.

- Maybe adding the current intent to the message model is better

- If users adds a delivery location for a meal and it is outside the current location we might refuse and then update their city and recommend 2 meals in that new location to them

- Where we strictly expect json and get plain text will respond strictly to them and resend the intended message/action to them

- Aid to reduce the prompt size and to (for cost) or process a number of items at once like people in same city

- Remember to exclude recommendations for the previous day and also tell the llm time of day as well

- Consider building all the recommedation for the pay for a particular user with one prompt and only rebuild it if the user updates preference or health or something like that

- Factor in timezone very important

- Always check on each request that the user has filled all the required fields eg city (gotten from delivery address), preferences, health etc

- Generate all the recommendations for a week and only refetch if they update something crusial

- After this point and click method I will also support them just typing stuff So if they type just text and they are not a just created user - if they reply to a message we know it's that message they are requesting an action else if they don't we can use the last bot message with an intent as the target message. Then we use AI to analyze the options and see what option the user is referring to and then perform that action


- I will just get the user info we need then put it in the system prompt and tell the LLM to ask the user for it after it is done with the current user request

----- Implement two step tool call to reduce the input tokens passed into the call make first call to kind of determine what category of tools to load then make second call to choose the exact tool then call it eg. first might be location -> then the second chooses update location

- Use action reply to say get page 2 etc
- Since bot messages can get long add a field called llm content if set use it instead of the actual message eg ids then for content let the llm something was cropped etc
- I will pass in just the names of the functions an tell the llm bases on the input what functions does it think it needs to call then when it lists it i will only pass the tools listed in the next llm call so it can select and call it with the arguments if any
- I will enforce the first llm must pick exactly 2 tools best related tools then i will pass those 2 into it. so even if the prompt doesn't look like it needs a tool it most pick something related just incase


- Find a way to use this for food recommendation: Embedding-based filtering
-- I could create the meals in a city embedding and then later filter out to get top options

- Implement the order flow with whatsapp flow and allow for update of delivery address before order creation.
- Implement meal reorder with the order code or a recommendation, also maybe by find and order eg. order rice and beans
- When order is complete send them a order review whatsapp form flow
- Improve recommendation and use cronjon to send them remember to check free service window. 
- Add Whatsapp flow for withdrawal
